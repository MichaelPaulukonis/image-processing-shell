TODO:

build a simple webserver attached to an arbtrarh location in local filesystem.

Allows for the tagging/renaming of image files.

Display thumbnails for a page of images (more above/below or other paging mechanism)
Allow for sinlge-image selection (Default mode) or multiple selection with check-marks on the thumbnails
Select one or more pre-propulated tags.
App will rename the file using tags in a specified orger (alphabetical, probably?), check for existing names, and rename
User should be able to "add" new tags, which will persist through the next session(s)

Use one of the cursor filee to plan ti